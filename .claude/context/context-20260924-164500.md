# Context — 2026-09-24 16:45:00 UTC

## Session goal
Follow-up to the 15:53 session. No code changes this session — purely
repo-hygiene / folder reorganization, done at the user's direction, mostly
executed by the user pasting terminal commands I provided (I cannot touch
their real filesystem directly; this repo's working copy lives on their
machine, not in my sandbox).

## What happened
1. After the 15:53 session's fixes, the user ran my suggested commands to
   scaffold `context/` and `.claude/memory/` and paste in the first
   context/memory files. A heredoc paste got interrupted mid-way in their
   terminal, but the files still landed correctly in the end.
2. When listing `.claude/memory/`, we discovered it already contained
   FIVE pre-existing files from earlier, unrelated sessions that predate my
   involvement entirely (never seen in my sandbox clone):
   - `architecture.md` — stack decisions, schema documentation, folder conventions
   - `context.md` — product/project brief (users, entities, workflows, design direction)
   - `gotchas.md` — environment quirks (drizzle-kit .env.local loading, Vercel/Neon
     env var scoping)
   - `MEMORY.md` — a running, append-only session log (3 entries: initial scaffold,
     DB setup, DB verification)
   - `debug-reports/` — empty directory
   These are legitimate, valuable project history — NOT garbage from a broken
   paste. They came from a prior Claude Code (or similar) session on the
   user's machine that used a different memory convention (a handful of
   named persistent docs + one running log) rather than the timestamped
   `memory-<timestamp>.md` snapshot convention used in the 15:53 session.
3. User clarified the desired convention: `.claude/memory/` should contain
   ONLY timestamped `memory-<timestamp>.md` snapshot files — nothing else.
   Persistent reference docs (architecture, product context, gotchas, debug
   reports) belong in a separate `docs/` location, and `MEMORY.md`'s 3
   log entries should be split into individual timestamped snapshot files
   consistent with the rest of `.claude/memory/`.
4. User further clarified: `context/` and `docs/` should both live under
   `.claude/` rather than at repo root — i.e. everything session/agent
   -related nests under one `.claude/` directory:
   ```
   .claude/
     memory/    <- timestamped memory-<timestamp>.md snapshots ONLY
     context/   <- timestamped context-<timestamp>.md session narratives
     docs/      <- persistent reference docs (architecture.md, context.md,
                    gotchas.md, debug-reports/) that don't belong to any
                    single timestamped session
   ```

## Actions taken (via commands handed to user, since I can't edit their
## local filesystem directly)
1. Moved `docs/architecture.md`, `docs/context.md`, `docs/gotchas.md`,
   `docs/debug-reports/` OUT of `.claude/memory/` into a new `docs/` folder
   (via `git mv`, preserving history).
2. Split `MEMORY.md`'s 3 entries into:
   - `.claude/memory/memory-20260924-143000.md` (Session 1: scaffold)
   - `.claude/memory/memory-20260924-150000.md` (Session 2: DB setup)
   - `.claude/memory/memory-20260924-152000.md` (Session 2 cont'd: verified/documented)
   Removed the original `MEMORY.md` (`git rm`).
3. This session: moved `context/` -> `.claude/context/` and `docs/` ->
   `.claude/docs/` (both via `git mv`).

## Final intended structure (as of this session)
```
.claude/
  memory/
    memory-20260924-143000.md   (Session 1: scaffold)
    memory-20260924-150000.md   (Session 2: DB setup)
    memory-20260924-152000.md   (Session 2 cont'd)
    memory-20260924-155333.md   (Session 3: fixed reported TS2307 bug +
                                  misplaced route.ts files + middleware typo)
    memory-20260924-164500.md   (this session: reorg only — see below)
  context/
    context-20260924-155333.md  (Session 3 full narrative)
    context-20260924-164500.md  (this file)
  docs/
    architecture.md
    context.md
    gotchas.md
    debug-reports/
```

## Nothing code-related changed this session
No source files touched. Product/technical status is exactly as left at the
end of the 15:53:33 session — see `.claude/memory/memory-20260924-155333.md`
and `.claude/context/context-20260924-155333.md` for the actual outstanding
engineering work (`.env.example`, `/login` page now urgent since middleware
is live, seed admin user, product UI/API scaffolding).

## State: LOCAL ONLY, not committed/pushed
All moves were done via `git mv`/`git rm` so history is preserved once
committed, but as of this file being written nothing has been committed to
the repo yet.
