# Context — 2026-09-25 22:22 (pagination, sidebar, dashboard)

## Completed in this continuation

- Shifted the application brand from maroon/neutral to MMC blue + red: blue is
  used for primary controls, focus, navigation, and links; red remains a
  purposeful logo accent and retains its status/error meaning. Added a cool
  blue-gray light canvas and updated the shared card border and selection tint.

- Added a shared accessible pagination control and wired 10-row pages into
  registered interns, authorized accounts, batches, leave records, and the
  attendance report's intern breakdown. Search pagination resets to page one;
  deleting rows clamps the displayed page to the new range.
- Made the desktop navigation sidebar viewport-height sticky, with the link
  area scrollable and the settings/logout section held at the bottom. Added a
  restrained maroon accent and refined active/hover navigation surfaces.
- Further refined desktop sidebar hierarchy: branded campus header, grouped
  Workspace/Insights/Administration links, inset icon tiles and active-page
  marker, account identity/role panel, and an accessible theme toggle in the
  pinned footer. Mobile navigation behavior is unchanged.
- Added a compact batches CSV export action with From/To batch selectors; the
  downloaded file includes batch dates, intern counts, current status, and
  today's date in the filename.
- Added an attendance-rate progress indicator and adjusted dashboard content
  proportions.

## Super-admin and seeded-account follow-up

- Super admins have full app access and may create super-admin accounts.
  Every super-admin row is view-only in Staff Accounts: it cannot be edited,
  password-reset, or deleted by any account. A super-admin can change their
  own password from Settings. Role checks resolve the live database role, and
  account mutations exclude super-admin targets in SQL for defense in depth.
- Migrations `0002_superadmin_role.sql`,
  `0003_attendance_department_presence.sql`, and
  `0004_preserve_history_on_account_removal.sql` were applied to the Neon DB
  configured in `.env.local` on 2026-09-25 to resolve dashboard query errors.
  Read-only verification found all five migrations and reran the failing
  attendance range query successfully (137 rows). This does not establish
  whether Vercel Production points at the same database.
- Exact demo usernames `staff1`, `staff2`, `staff3`, and `admin` were removed
  from the Neon DB configured in `.env.local`. Attendance/leave history
  remains; deleted actor references become null and display “Former account”.
  Existing `mahesh` was updated to Dr.Mahesh Raj Sigdel / superadmin through
  the hidden password prompt per the user's prior request. Vercel Production
  may use a separate database and has not been inspected or changed. Stored
  username convention lowercases MAHESH to `mahesh`.
- `seed-admin.ts` is an account bootstrap utility, not a fixed demo-user seed.
  `seed.ts` seeds labeled demo attendance data and reuses an existing operator.

## Attendance department presence and Bikram Sambat dates

- Attendance has a distinct explicit `present` record only when the intern is
  present at another department. Department values are Cardiology,
  Dermatology, Psychiatry, and Other. Ordinary present remains implicit.
- The status/dept pair is validated, stored on attendance records, displayed
  in the attendance log and intern history, and excluded from absence/late/
  leave totals so the intern still counts present.
- Migration `0003_attendance_department_presence.sql` is applied to the
  `.env.local` Neon DB; production migration state has not been verified.
- User-facing dates use numeric Bikram Sambat (`DD/MM/YYYY`), including
  posting periods and recorded attendance. Calendar month label is `MM/YYYY`.
  Native Gregorian controls are replaced by numeric BS day/month/year
  selectors. ISO values remain the database/API format.

## Verification

- `npm run lint`, `npx tsc --noEmit`, and `git diff --check` passed after the
  attendance/date localization work. `npm run build -- --webpack` also passed.
  Default Turbopack build hit an OS helper-process permission error in this
  sandbox; Webpack production build completed all routes successfully.
- The follow-up superadmin/pagination/history-attribution work passed lint,
  TypeScript, and `npm run build -- --webpack` on 2026-09-25.
