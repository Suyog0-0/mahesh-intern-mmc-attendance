# Context — 2026-09-25 16:20 Asia/Kathmandu

## Session goal

Polish attendance and leave interactions requested after the previous UI pass:
hide already-absent interns from the attendance picker, refine global toast UI,
compact attendance-report filters, improve leave records, replace visible
Edit/Delete/View action text with accessible icons, and add a password reveal
control when creating staff accounts. The calendar was explicitly out of scope.

## Work completed

- Attendance suggestions now exclude interns already marked absent for the
  selected date; late and leave entries remain searchable.
- Refined the shared toast stack with a more considered top-right container,
  tighter spacing, status rail, larger status glyph, and subtle ring/shadow.
- Rebuilt attendance report filters as a compact single-row batch/from/to
  toolbar, with accessible field labels, icons, and distinct restrained colors.
- Added an eye action to desktop report rows and replaced mobile “History” text
  with an eye affordance.
- Tightened logged leave records with a count badge, compact identity row,
  blue date-range panel on mobile, and explicit view/delete icon buttons.
- Added accessible icon-only profile/edit/delete controls in intern listings,
  icon-only edit/reset-password/delete actions for batch and staff mobile cards,
  and a show/hide control for the initial staff password.
- Kept the calendar files unchanged in this pass. No schema changes or new
  dependencies were introduced.

## Verification

- `npm run lint`: passed with two existing unused-variable warnings in
  `scripts/seed.ts` (`inArray`, `notInArray`).
- `npx tsc --noEmit`: passed.
- Earlier `git diff --check` reports trailing whitespace in existing dirty
  `scripts/seed.ts` and `src/components/student-drawer.tsx`; those unrelated
  lines were not modified.

## Scope note

The worktree already contained unrelated changes from earlier UI requests;
preserve those, especially the calendar updates, seed script, and student
drawer. The current pass only adds the requested targeted refinements.
