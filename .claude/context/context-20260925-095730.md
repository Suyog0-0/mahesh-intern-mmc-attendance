# Context — 2026-09-25 09:57 UTC

## Session goal

Continue the mobile UI work and address app-wide layout defects, attendance
success feedback, oversized controls, horizontal overflow, and the broken
calendar. User also asked for ongoing updates in this project's `.claude/`
notes.

## Work completed

- Attendance marking now shows an accessible success confirmation after the
  API succeeds and plays a short two-note Web Audio chime. This also runs when
  an attendance exception is cleared and the intern is marked present. The
  success confirmation appears inside the attendance modal during rapid entry
  and on the page after the modal is closed. Audio is best-effort and silently
  unavailable if the browser/device blocks it.
- Attendance controls now fit in one compact mobile row, use Kathmandu-local
  today for the date maximum, and attendance exception rows wrap cleanly.
- Fixed the mobile table rule so it overrides each table's 600px minimum width.
  The intern directory, leaves, reports, batches, and staff account tables now
  fit the viewport as labeled stacked rows. Removed the hidden duplicate mobile
  report-card markup.
- Removed global mobile button font enlargement, which was inflating labels
  across icon and navigation controls. Editable fields retain 16px sizing to
  prevent iOS focus zoom. Date widths are now scoped to report filters rather
  than forced across the app.
- Rebuilt the calendar responsive layout: seven-column grid, six-week
  alignment, compact mobile day cells and status markers, desktop issue counts,
  accessible per-day summaries, clear month controls, and valid-month parsing.
- Reduced mobile page gutters and set the main content to `min-w-0` to avoid
  flex/grid intrinsic-width overflow.

## Verification

- `npm run lint`: passed with five existing warnings (unused imports in
  `scripts/seed.ts`, `batches-manager.tsx`, and `students-manager.tsx`).
- `npx tsc --noEmit`: passed.
- `npm run build`: passed after a sandbox worker-process restriction required
  an approved rerun with process permissions. All 25 static page-generation
  steps completed and routes were generated.
- Scoped `git diff --check` on the files changed in this continuation passed.
  A repository-wide check still reports whitespace in pre-existing user
  changes to `scripts/seed.ts` and `student-drawer.tsx`; these were left alone.
- No database or schema changes were needed.

## Notes for continuation

- The working tree already contained broad modifications before this
  continuation. Preserve those changes; this session only edited the
  attendance board, calendar view/page, responsive styles, report view,
  navigation shell, and these timestamped notes.
- A real-device visual check is still useful for Safari date-picker rendering
  and whether synthesized success audio is enabled by the device.
