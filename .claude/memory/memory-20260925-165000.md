# Memory — 2026-09-25 16:50 (Empty states / UI-review seed)

- Empty attendance batches now prompt “Add interns first”; “All Interns Present”
  appears only if the batch contains interns and has no exception rows.
- Report filters aligned to compact controls with consistent heights; retained
  mobile-safe 16px date text.
- Updated empty-data copy and refined action icon colors (blue edit, red delete,
  sky-blue password reset, neutral profile view).
- `scripts/seed.ts` now adds repeatable UI-review records (200 demo interns,
  varied attendance and leaves) without clearing existing rows or creating
  users. It is ready but not executed pending confirmation that DATABASE_URL is
  local/staging.
- Lint, TypeScript, and scoped whitespace checks passed. Calendar/schema
  untouched.
