# Memory — 2026-09-25 22:22

- Reusable page control: `src/components/pagination.tsx`; current capped list
  size is 10. Students, staff accounts, batches, leaves, and report breakdown
  are paginated on desktop/mobile lists.
- Batches export is client-generated CSV over an inclusive selected range in
  the existing batch ordering; file name is date-stamped.
- Desktop sidebar is `sticky` and viewport-height; its navigation scrolls
  independently, and account/settings/logout stay visible at the bottom. It
  uses Workspace/Insights/Administration groups, campus-branded header,
  marked active link, role/account identity panel, and desktop theme toggle.
- Super-admin policy: full app access; superadmin account rows are view-only
  (cannot be edited/reset/deleted); an authenticated superadmin can create
  another and change their own password in Settings.
- Exact demo usernames `admin`, `staff1`, `staff2`, and `staff3` were deleted
  from the `.env.local` Neon DB; attendance/leave history was preserved with
  null actor references (“Former account”). Existing `mahesh` was updated to
  Dr.Mahesh Raj Sigdel / superadmin. Production may use a separate DB and was
  not changed.
- Explicit department attendance uses status `present` plus nullable enum
  `department` (Cardiology, Dermatology, Psychiatry, Other). It is listed as a
  present update but ignored by absence/late/leave metrics. Migration 0003 is
  applied to the `.env.local` Neon database.
- All user-facing dates now use numeric Bikram Sambat (`DD/MM/YYYY`), including
  posting periods, attendance rows, reports, calendar month labels, and CSV.
  Native Gregorian date inputs are replaced by numeric BS selectors. ISO
  database/API values remain for date comparisons and persistence.
- Attendance log row action directly clears an exception and marks present,
  with optimistic UI and a success/error toast. Rows reflow on mobile. Report
  filters use a two-column mobile / three-column desktop layout.
- Verification for this work: lint, TypeScript, and diff whitespace check pass.
# Follow-up: protected superadmins, actor attribution, pagination

- Shared Pagination now uses compact accessible page buttons/range text and
  hides entirely below 10 rows; wired Students to shared pagination as well.
- Staff Accounts renders superadmins as view-only, with a details modal.
  No user (including a superadmin) can edit/reset/delete a superadmin record;
  a superadmin can still change their own password in Settings. Bootstrap
  utility accepts an interactive hidden password and updates the name too.
- Actor refs `attendance_records.marked_by` and `leaves.created_by` are nullable
  `ON DELETE SET NULL` in schema and generated migration 0004. History/logs
  label null actors “Former account”. User selected retaining attendance and
  leave rows during demo-account cleanup.
- Migrations 0002–0004 and the exact demo-account cleanup were applied to the
  `.env.local` Neon database; the attendance range query passed verification.
  Production DB access remains unavailable and may point to a separate Neon DB.
- Validation passed: ESLint, TypeScript, `git diff --check`, and Webpack
  production build (`npm run build -- --webpack`).
