# Memory — 2026-09-25 17:16 (UI polish / deployment check)

- Mobile input copy is compact at rest and 16px on focus to avoid iOS zoom;
  report filters use smaller labels and aligned touch-size controls.
- Toast cards use distinct restrained status colors instead of white surfaces.
- Leave range says From / To with compact styling; report exports include
  today's app-local date in the filename.
- Added a small app-level back button for non-dashboard routes.
- Dashboard now summarizes average attendance and exceptions over the latest
  seven calendar days instead of “Batch to date.”
- Improved staff account records and empty-batch guidance across dashboard and
  leave entry.
- Vercel is not linked in this workspace; local `.env.local` points to a remote
  database but may not be Vercel Production. No production writes/deploys.
- Lint, TypeScript, and scoped whitespace check pass. Build is blocked by
  Turbopack worker process permission (`Operation not permitted`).
